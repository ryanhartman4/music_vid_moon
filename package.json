{
  "name": "music-vid-moon",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Procedurally animated, beat-synced music videos painted in code (Canvas2D + Playwright + ffmpeg).",
  "scripts": {
    "sheet": "node render.mjs --sheet=68.6,69,69.4 --cols=3 --out=out/check.jpg",
    "frames": "node render.mjs --frames=0:158.8 --workers=2",
    "encode": "node render.mjs --encode --out=out/take_me_to_the_moon.mp4"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}